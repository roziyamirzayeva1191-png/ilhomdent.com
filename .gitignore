node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Secrets — never commit
.env*
!.env.example

# Runtime data (database, backups, logs, user uploads)
data/
backups/
logs/
public/uploads/
site_data_store.json
site_data_store.json.imported.bak

# Xavfsiz deploy-signal papkasi (runtime signal fayllar, kodga tegishli emas)
deploy-signal/

# SSL bootstrap paytida yaratiladigan vaqtinchalik zaxira (DEPLOYMENT.md, 3-bosqich)
nginx.conf.full.bak
